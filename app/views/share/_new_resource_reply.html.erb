<%= form_tag({controller: "resource_blogs",action: "deal_reply"}, {method: "post"}) do%> 
    <%= render "share/feed_reply" %>
    <div class="col-xs-12">
        <%= text_area_tag :content %>
        <%= radio_button_tag :receiver_id, @resource_blog.user_id, checked = true %>回复帖子
        <%= submit_tag "回复",class: "btn btn-primary"%>
    <div>
<% end %>