<%= form_with(model: file_transfer, local: true) do |form| %>
  <% if file_transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_transfer.errors.count, "error") %> prohibited this file_transfer from being saved:</h2>

      <ul>
      <% file_transfer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file_path %>
    <%= form.text_field :file_path %>
  </div>

  <div class="field">
    <%= form.label :introduction %>
    <%= form.text_field :introduction %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
